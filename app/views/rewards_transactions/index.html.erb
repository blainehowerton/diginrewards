<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <link rel="stylesheet" href="/resources/demos/style.css">

</head>
<body>

<div class="jumbotron">
      <div class="container">
        <h1>Rewards Transactions</h1>
        <p>Viewing all transaction balances.</p>
      </div>
    </div>

<div class="container">
  <p><%= notice %></p>
  <p>PLEASE NOTE:  If balances for retailers show a positve balance, a withdrawl needs to be made (so the balance is at $0.00).  This is the amount the retailer account owes Dig In for Transaction fees.</p>

<div class="table-responsive">
  <table class="table">
  <thead>
  <tr>
    <th>Retailer ID</th>
    <th>Retailer Name</th>
    <th>Deposits</th>
    <th>Withdrawls</th>
    <th>Balance</th>
    <th colspan="3"></th>
      </tr>
    </thead>
  <tbody>
  <% @rewards_balances.each do |item| %>
      <tr>
        <% id = item.retailer_id %>
        <% name = item.retailer.name %>
        <% total_credits = item.total_credits || 0 %>
        <% total_debits = item.total_debits || 0 %>
        <% balance = total_credits - total_debits %>
        <td><%= id %></td>
        <td><%= name %></td>
        <td><%= number_to_currency(total_credits) %></td>
        <td><%= number_to_currency(total_debits) %></td>
        <td><%= number_to_currency(balance) %></td>
    <% end %>
  </tr>
  </tbody>
</table>


</div>

<br>
<br>

<%= yield %>

</body>

<hr>
<%= link_to 'Home', "/" %> | 
<%= link_to 'Transaction Dashboard', "/transactions" %> |
<%= link_to 'New Rewards Transaction', new_rewards_transaction_path %> |
<%= link_to 'Database Admin', "/admin" %>
<br>

</div>

</html>